<!DOCTYPE html>
<html>
  <head>
    <% include ../static/partials/head.ejs %>
  </head>
  <body>
    <% include ../static/partials/navbar.ejs %>

    <main class="container">
      <h1><%= flair.name %></h1>
        <a href="/topics/<%= flair.topicId %>/posts/<%= flair.postId %>/flairs/edit/flair.id" class="btn btn-warning">Edit</a>
        <a href="/topics/<%= flair.topicId %>/posts/<%= flair.postId %>/flairs/new">New Flair</a>

        <form style="display:initial" action="/flairs/<%=flair.id%>/destroy" method="post">
          <button type="submit" class="btn btn-danger">Delete</button>
        </form>

      <h1><%= flair.color %></h1>
    </main>

    <% include ../static/partials/baseScripts.ejs %>

  </body>
</html>
